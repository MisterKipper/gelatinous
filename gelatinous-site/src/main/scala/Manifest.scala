package gelatinous.site

import template._

object Manifest extends gelatinous.Manifest {
  // val posts = List(Post("----\ntitle: Post 1\nauthor: Me\n----\n# 123\n456\n"), Post("----\ntitle: Post 2\nauthor: Me\n----\n# ABC\nxzy\n"))
  val singlePages = List(AboutMe)
  // val collections = List(Index(posts.map_.digest))
}
