package gelatinous

import org.scalatest.flatspec.AnyFlatSpec

// import cats.effect.IO
// import gelatinous.Util
// import java.nio.file._

class UtilSpec extends AnyFlatSpec {
//   val source = Paths.get("src/test/resources/test-dir/")
//   val target = Paths.get("target/test/test-dir/")
//   Util.cleanDirectory(target).unsafeRunSync()
//   Files.deleteIfExists(target)

//   "Directory operations" should "work" in {
//     assert({
//              for {
// _ <- IO.delay(Files.)
// }
//            })
//     assert({
//       for {
//         _ <- Util.cleanDirectory(target)
//         files <- Util.listDirectory(target)
//         isEmpty <- IO.delay(files.isEmpty)
//       } yield isEmpty
//     }.unsafeRunSync())
//   }

  // "Copying a directory" should "work" in {
  //   Util.cleanDirectory(target)
  //   Util.copyDirectory(source, target)
  //   // assert(Util.directoriesEqual(source, target))
  // }
}
